# requirements.txt (root)
-r dashboard/requirements.txt

# plus any shared/runtime deps used by pipeline and ops tooling:
pydantic>=2.0
httpx>=0.27
tenacity>=8.0
structlog>=24.0
opentelemetry-api>=1.0
opentelemetry-sdk>=1.0
