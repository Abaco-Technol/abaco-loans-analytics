# Dashboard runtime dependencies (authoritative for App Service)
-r dashboard/requirements.txt
-r requirements.pipeline.txt

# Multi-Agent System Dependencies
# (Keep versions flexible to avoid conflicts with dashboard requirements)
hubspot-api-client>=9.2.0
python-dotenv>=1.0.1
langchain>=0.3.13
langchain-openai>=0.2.14
langchain-anthropic>=0.3.7
langchain-community>=0.3.13
langchain-core>=0.3.28
