# services/payments/AGENTS.override.md
## Payments service rules
- Use `make test-payments` instead of `npm test`.
- Never rotate API keys without first notifying the #security-incident Slack channel and following the "Payments API Key Rotation" runbook (page the on-call via PagerDuty for urgent rotations).
